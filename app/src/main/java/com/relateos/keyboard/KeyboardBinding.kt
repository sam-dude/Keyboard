package com.relateos.keyboard

import android.view.View
import android.widget.Button

/**
 * Custom binding class that works with both keyboard layouts
 */
class KeyboardBinding(val root: View) {
    // This class only needs to keep track of the root view
    // We'll use findViewById for accessing specific buttons as needed
}